{
  "name": "@owo/orchestration",
  "version": "0.1.0",
  "description": "Background task orchestration with toast notifications for OpenCode",
  "files": [
    "dist"
  ],
  "type": "module",
  "types": "./src/orchestration.ts",
  "exports": {
    ".": {
      "types": "./dist/orchestration.d.ts",
      "default": "./dist/orchestration.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "make": "bun build src/orchestration.ts --outdir dist --target bun --external '@opencode-ai/*' --external '@owo/*' --external 'zod' && tsgo --emitDeclarationOnly",
    "prepublishOnly": "bun run make",
    "compile": "tsgo --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-ai/plugin": "catalog:",
    "@opencode-ai/sdk": "catalog:",
    "@owo/config": "workspace:*"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260120.1",
    "bun-types": "1.3.6"
  }
}
