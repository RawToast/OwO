{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@owo/github-reviewer",
  "version": "0.0.1",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "make": "bun build ./src/index.ts --outdir ./dist --target node --format esm --external '@opencode-ai/*' --external 'zod' --external '@octokit/*' --external 'parse-diff'",
    "compile": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "parse-diff": "^0.11.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "catalog:",
    "@opencode-ai/sdk": "catalog:",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  }
}
