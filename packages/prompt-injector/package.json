{
  "name": "@owo/prompt-injector",
  "version": "0.1.0",
  "description": "Config-driven prompt injection plugin for OpenCode",
  "files": [
    "dist"
  ],
  "type": "module",
  "types": "./src/prompt-injector.ts",
  "exports": {
    ".": {
      "types": "./dist/prompt-injector.d.ts",
      "default": "./dist/prompt-injector.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "make": "bun build src/prompt-injector.ts --outdir dist --target bun --external '@opencode-ai/*' --external '@owo/*' --external 'zod' && tsgo --emitDeclarationOnly",
    "prepublishOnly": "bun run make",
    "compile": "tsgo --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-ai/plugin": "catalog:",
    "@opencode-ai/sdk": "catalog:",
    "@owo/config": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
