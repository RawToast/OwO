{
  "name": "@owo/tools",
  "version": "0.0.1",
  "description": "Shared tools for owo plugins",
  "files": [
    "dist"
  ],
  "type": "module",
  "types": "./src/tools.ts",
  "exports": {
    ".": {
      "types": "./dist/tools.d.ts",
      "default": "./dist/tools.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "make": "bun build src/tools.ts --outdir dist --target bun --external '@opencode-ai/*' --external '@owo/*' --external 'zod' && tsgo --emitDeclarationOnly",
    "prepublishOnly": "bun run make",
    "compile": "tsgo --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@owo/config": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "1.1.40",
    "@opencode-ai/sdk": "1.1.40"
  }
}
